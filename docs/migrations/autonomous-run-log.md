# Autonomous run log (2026-07-14)

Decision trail for the priority program derived from the 7-agent codex/sol investigation.
Exit predicate: each unit ends green (`tsc -b` = 0, `npm run lint` = 0 errors, `npm run test:unit` = 174+, `npm run test:e2e:emulator` = all pass), reviewed, and committed. Priority order: hygiene → T1 → T2 → T3 → T4/Phase3 → T5/Phase1b → later phases.

Ground truth this session (verified, not self-reported):
- `tsc -b` exit 0.
- Unit: 174 passed.
- E2E: 19/19 passed (28.6s) with brew openjdk 26 (`JAVA_HOME=/opt/homebrew/opt/openjdk`).

Model routing (from user goal): sol high / terra medium for mechanical, sonnet-5 for UI, opus for validation + adversarial review.

| Unit | Change | Predicate moved | Notes |
|---|---|---|---|
| Hygiene | Removed stale locked worktree `agent-a3d89d3efda68007a` (merged ancestor `e4965a9`, clean); `.claude` added to eslint ignore + `.gitignore`; deleted 10 stale/dangerous Python scripts (incl. `delete_quran_collections.py` which deletes the live root `quran/`, and `migrate_to_event_structure.py` live-by-default) + `requirements-viewer.txt` + 3 tracked `.pyc`; dropped dead `start-quran-web` npm script; fixed 7 `no-explicit-any` lint errors; added `.github/workflows/ci.yml` (Node 20 + Temurin 21, runs tsc/lint/unit/e2e). Kept Quran-storage scripts (`get_quran_images.py`, `upload_quran_images.py`, `rename_quran_pages.py`) until Phase 2. | lint 16→0 errors; tsc 0; unit 174; e2e 19/19 | `useFirestoreListener` registry typed `unknown` (heterogeneous by key) with boundary casts — file is slated for T4 redesign. Committed `0af5031`. |
| Firebase 11→12 | Bumped `firebase ^11.0.2` → `^12.16.0` (@firebase/firestore 4.16.0). Researched the v12 breaking changes: Node 20 floor (have 24; CI on 20), ES2020 (already targeted), and removal of `firebase/vertexai`→`firebase/ai` / Vertex AI symbols — none used here (app imports only firebase/app, firebase/analytics, firebase/firestore). Drop-in: only package.json + lockfile changed, no code edits. | tsc 0; lint 0; unit 176; e2e 22/22 (real Chromium + emulator + fb12, incl. live onSnapshot updates) | firebase-admin already 14.1.0. Firestore listener/query/write paths all exercised green. |
| Phase 2 eval (Quran assets) | Design/evaluation doc `docs/migrations/phase-2-quran-assets.md` (no code changed). Current: 604 page images are base64 blobs in root Firestore `quran/{n}` (anti-pattern: 1 MiB cap, no CDN, ~33% bloat). **Verified a real live render bug**: uploader writes a full `data:image/png;base64,…` URI, `QuranViewer.tsx:92` prepends the prefix again → doubly-prefixed broken `<img>`. Recommendation: serve immutable pages as static assets via **Firebase Hosting** (`/quran/{edition}/{page}.webp`, 1-year immutable cache, sha256 manifest) — already-configured CDN, no new vendor; R2 the strongest alternative if scope grows. Blocking decision: the 604-page source set isn't in the repo (or must be exported from live Firestore) and its redistribution license confirmed. | doc only | Evaluation, not implementation, per request. Double-prefix bug worth a standalone one-line fix or fix-during-migration. |
| T3b (big-screen range) | The audience big-screen showed a hardcoded "Juz 1 - 20" for **every** participant regardless of category. Replaced with a config-driven range derived from the participant's category question slots: a Juz range when slots carry `sourceJuzRange`, otherwise the page span (e.g. CAT_A → "Pages 3 - 101"); "-" when the category is absent from config. | tsc 0; lint 0; unit 176; e2e 22/22 | Other T3b items (unknown-category error rows, listener-error UI state, ScoreDetailsDialog thresholds) remain as lower-value polish. |
| Phase 7 (i18n placeholders) | Filled the literal `[MISSING]` (13, en) / `[FALTA]` (15, pt-PT) placeholder translation values that rendered to jurors/operators verbatim — jury toast messages, question-nav buttons, participant-status reset UI, empty-search text, aria-labels. Translations by sonnet-5 (European Portuguese, matching the Lisbon origin), verified value-only (no keys added/removed), JSON valid, 0 placeholders left. | tsc 0; lint 0; unit 176; e2e 22/22 | Bounded, user-facing. Remaining Phase 7 (fast participant-add, ParticipantForm hardcoded strings, Excel import) not done. |
| T4 (listener lifecycle) | **Scoped to correctness fixes; Firebase 11→12 bump deferred (user: focus listener fixes), auth deferred (user decision).** (1) `useFirestoreListener` callback-identity leak: it added `callbackRef.current` (a fresh onData closure each render) to the shared registry Set on subscribe but deleted a *different* `callbackRef.current` on cleanup — leaking stale callbacks that fire on every snapshot. Fixed with a stable per-instance `subscriber` wrapper (calls latest onData, constant identity), captured into a local at effect top so add/delete match. Removed the dead `refresh()`/`unsubscribeRef` (no consumer called it). (2) `useUpdateActiveQuestion` + `useUpdateParticipantQuestion` optimistic updates used the unscoped `["activeParticipant"]` key while `useActiveParticipant` reads/writes `["activeParticipant", currentEvent]` — so optimistic UI silently did nothing until the Firestore round-trip. Event-scoped all keys. | tsc 0; lint 0; unit 176; e2e 22/22 | Opus review: no blocking issues (ran N-instance + StrictMode + stale-object attacks). Deferred: `useParticipants` O(n²) rescan-per-snapshot (perf, risky — small events), and PerformanceMonitor "Cleanup All" resubscribe footgun (Phase 8). Non-blocking note: the two listener cleanup branches use captured-object vs fresh-get lookup (equivalent today, pre-existing). |
| Chrome+Firebase validation | Drove the live app (Vite dev on 4173 wired to the seeded Firestore emulator) in Claude-in-Chrome and modified Firestore via the emulator REST API. All passed: (1) `/participants` no event → "No event selected" gate, 0 tables; (2) `/participants?event=demo-2026` → table renders + `localStorage['fip-hifz.currentEvent']` persisted; (3) `/quran-page` **no param** → restored demo-2026 from localStorage, URL rewritten to `?event=demo-2026`, **active participant Ahmad detected** (Phase 6 fixed); (4) PATCH participant `isActive` via emulator REST → browser live-updated Ahmad→Bilal (page 69) **with no reload** (onSnapshot reactivity); (5) `/jury?event=demo-2026` → JuryScoringPanel renders under ready gate (config-driven CAT_B tabs + Hifdh/Tajweed/Presentation/Bonus); (6) `/jury?event=unconfigured-event` → fail-closed gate, scoring panel does NOT mount (proves T3a gating). | 6/6 live checks passed | Honors the "test pages in Chrome + firebase-cli/firestore" requirement. |
| T3a | Jury scoring hooks (`useJuryScores`, `useJuryNavigation`) previously ran at the top of the `/jury` route **outside** `<EvaluationConfigGate>` (only `ScoreForm` was gated), so they read/wrote V2 Firestore + built state before config was `ready`. Extracted a new `JuryScoringPanel` that owns both hooks + `ScoreForm`, rendered strictly inside the gate. | tsc 0; lint 0; unit 176; e2e 22/22 | Opus review: nothing blocking; it caught that my first attempt (debounce-cancel-on-unmount) was a **data-loss regression** (dropped the last ≤500ms edit on logout/route-exit, which pre-T3a persisted via the post-unmount timer). **Removed** that cleanup per the review; documented why. Non-blocking deferred to T3b: unknown-category error surfacing, listener-error UI state, big-screen hardcoded "Juz 1-20", ScoreDetailsDialog thresholds. |
| T2 | **Rescoped after reading the code.** The "shared strict V2 decoder" the plan called for is already implemented in `useParticipants.ts` (provenance via `hasCurrentProvenance` = configVersion + assignmentHash, duplicate-**doc** rejection, per-question completeness). The jury `useJuryScores` path only merges stored values into an *editable form* and re-saves through the validated `saveEvaluationScore`, so its merge is legitimate, not a scoring hole. The one genuine residual engine-contract gap: `scoreJury` double-scores if `assignedQuestionNumbers` contains a duplicate (unreachable via current callers — they pass `[1..questionCount]` — but the engine is a reusable pure boundary and its docstring promises uniqueness). Added a uniqueness guard + a non-finite guard in `scoreParticipant`, with 2 discriminating unit tests. Deferred config-authoring validation hardening (Area 1 #6/#7: discriminated cap shapes, unknown nested fields) as a separate follow-up. | tsc 0; lint 0; unit 174→176 | Small pure-function change; self-reviewed (both tests fail without the guards). |
| T1 | Removed the silent `currentEvent \|\| 'demo-2026'` fallback across 12 files (fail-loud guards: `throw` in RQ mutationFns, early-return in handlers — implemented by gpt-5.6-sol high, reviewed). EventContext no longer defaults a param-less non-root route to `demo-2026`; instead persists the last explicitly-selected event to `localStorage` and restores it (same-browser), else fails closed via the gate. This fixes Phase 6: an event-less `/quran-page` no longer shows the wrong event's absent participant. | tsc 0; lint 0; unit 174; e2e 21→22 incl. new `eventFallback.spec.ts` | Opus adversarial review found **2 BLOCKING**, both fixed: (1) guard sat before `e.preventDefault()` in JuryLogin/JuryForm → native form submit/reload on the newly-reachable null-event state; (2) localStorage is per-browser so the comment's "second screen = separate device" claim was corrected (separate device needs `?event=`, which in-app nav includes). Deferred (noted): `/admin` un-gated with silent no-op buttons → T3; `replaceState`-after-`beforeLoad` auth bypass → T5. |
